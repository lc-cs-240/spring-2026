# Note 14

## An example schema

Associated with this note you'll find a [sample schema](./registrar-schema.sql).

## Activity

* Create a user called `registrar` that has full access to a database of the
  same name (`registrar`) but no other privileges.
* Load the schema to construct the `registrar` database.
