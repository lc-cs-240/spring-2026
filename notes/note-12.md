# Note 12

## Components of a database management systems

* Users
* Applications
* Database management system (DBMS)
* Operating system (OS)
* Storage

## MariaDB REPL

* Find out how to start the database REPL installed with XAMPP on a Mac.
* Find the command to display a list of all databases.
* Find the command to display the list of tables associated with a particular
  database.
* Find the command to display the list of all columns associated with a
  particular table.
* Use the commands above to explore the content of this database management
  system. Anything noteworthy?
* Find the command to create a backup of a particular database.
* Create a simple SQL script to initialize a *new* database.
